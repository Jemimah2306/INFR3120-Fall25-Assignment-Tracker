<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <!-- Website logo or title -->
        <a class="navbar-brand" href="/">
            <img src="/images/logo.png" alt="Assignment Tracker Logo" height="35" class="me-2">
            Assignment Tracker
        </a>

        <!-- Menu button for mobile -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Collapsible navigation -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <!-- Home / Assignments List -->
                <li class="nav-item">
                    <% if (title == "Home") { %>
                        <a class="nav-link active" aria-current="page" href="/"><i class="fa-solid fa-lg fa-book"></i> Assignments</a>
                    <% } else { %>
                        <a class="nav-link" aria-current="page" href="/"><i class="fa-solid fa-lg fa-book"></i> Assignments</a>
                    <% } %>
                </li>

                <!-- Add Assignment -->
                <li class="nav-item">
                    <% if (title == "Add Assignment") { %>
                        <a class="nav-link active" aria-current="page" href="/tasks/add"><i class="fa-solid fa-plus"></i> Add Assignment</a>
                    <% } else { %>
                        <a class="nav-link" aria-current="page" href="/tasks/add"><i class="fa-solid fa-plus"></i> Add Assignment</a>
                    <% } %>
                </li>

                <!-- Assignment List -->
                <li class="nav-item">
                    <% if (title=="Events") { %>
                        <a class="nav-link active" aria-current="page" href="/tasks/list"><i class="fa-solid fa-calendar-check"></i> Assignments</a>
                    <% } else { %>
                        <a class="nav-link" aria-current="page" href="/tasks/list"><i class="fa-solid fa-calendar-check"></i> Assignments</a>
                    <% } %>
                </li>
            </ul>
        </div>
    </div>
</nav>